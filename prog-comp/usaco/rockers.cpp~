/*
  ID:   caizhuo2
  PROG: fence
  LANG: C++
*/

#include <iostream>
#include <fstream>
#include <cstdlib>
#include <algorithm>
#include <vector>
#include <queue>
using namespace std;

#define maxSong 20
int songs[maxSong];

int main() {
    int N, T, M;
    cin >> N >> T >> M;
    for (int i = 0; i < N; i++) cin >> songs[i];
    
}
