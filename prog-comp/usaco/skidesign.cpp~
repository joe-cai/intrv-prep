/*
  ID:   caizhuo2
  PROG: skidesign
  LANG: C++
*/

#include <iostream>
#include <fstream>
#include <cstdlib>
#include <algorithm>
#include <vector>
#include <queue>
using namespace std;

int cost(int h, vector<int>& nums) {
}

int main() {
    int N;
    cin >> N;
    vector<int> nums(N, 0);
    int h = INT_MIN, l = INT_MAX, idx_h = 0;
    for (int i = 0; i < N; i++) {
	cin >> nums[i];
	if (h < nums[i]) {
	    idx_h = i;
	    h = nums[i];
	}
	l = min(l, nums[i]);
    }
    for (int i = h; i >= l; i--) {
	
    }
}
